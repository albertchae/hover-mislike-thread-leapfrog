{
  "name": "app",
  "private": "true",
  "dependencies": {
    "esbuild": "^0.17.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "build": "esbuild app/javascript/application.jsx --bundle --sourcemap --outdir=app/assets/builds --public-path=assets"
  }
}
